name: Building recovery

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - master
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]

env:
  MANIFEST: https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-11
  DEVICE: kyoto
  DT_LINK: https://github.com/forforksake/android_device_motorola_kyoto
  DT_PATH: device/motorola/kyoto
  TARGET: bootimage
  VENDOR: motorola
  TZ: GMT
  
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
       - name: Checkout
         uses: actions/checkout@master

       - name: Cleaning Up Runner
         uses: rokibhasansagar/slimhub_actions@main

       - name: Initializing environment
         run: |
            sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php /etc/apt/sources.list.d
            sudo -E apt-get clean
            sudo apt update
            sudo apt install bc build-essential zip curl libstdc++6 git wget python gcc clang rsync flex curl bison liblz4-tool libncurses5 libncurses5-dev libsdl1.2-dev libssl-dev aria2 -y
            sudo curl --create-dirs -L -o /usr/local/bin/repo -O -L https://storage.googleapis.com/git-repo-downloads/repo
            sudo chmod a+rx /usr/local/bin/repo
            
       - name: Sync recovery source and device tree
         run: |
             mkdir work
             cd work
             repo init -u $MANIFEST --depth=1 --groups=all,-notdefault,-device,-darwin,-x86,-mips
             repo sync -c -q --force-sync --optimized-fetch --no-tags --no-clone-bundle --prune -j$(nproc --all)
             repo sync --force-sync
             git clone $DT_LINK --depth=1 --single-branch $DT_PATH
             
       - name: Build
         run: |
              cd work
              . build/envsetup.sh && lunch twrp_$DEVICE-eng && export ALLOW_MISSING_DEPENDENCIES=true && mka $TARGET
             
       - name: Upload recovery image 
         run: |
              cd work/out/target/product/$DEVICE
              curl -sL https://git.io/file-transfer | sh
              ./transfer gof boot.img && ./transfer gof ramdisk-,recovery.img
              ./transfer wet boot.img && ./transfer wet ramdisk-recovery.img

       - uses: actions/upload-artifact@v2
         with:
          name: recoveryzip
          path: work/out/target/product/*/boot.img

       - uses: actions/upload-artifact@v2
         with:
          name: recoveryimage
          path: work/out/target/product/*/ramdisk-recovery.img
